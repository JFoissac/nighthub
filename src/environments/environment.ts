export const environment = {
  production: true,
  /** Racine de l'API REST (préfixée par /api). */
  apiUrl: '/api',
  /** Origine serveur pour les endpoints hors /api (health, fenêtres OAuth). */
  origin: '',
};
