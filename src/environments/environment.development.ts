export const environment = {
  production: false,
  /** Racine de l'API REST (préfixée par /api), proxifiée vers le backend. */
  apiUrl: '/api',
  /** Origine serveur pour les endpoints hors /api (health), proxifiée aussi. */
  origin: '',
};
